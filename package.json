{
  "name": "jdiperi-portfolio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "start": "npm run build && node scripts/serve-static.mjs",
    "serve": "node scripts/serve-static.mjs"
  }
}
